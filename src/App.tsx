/**
 * Placeholder — the EXPERIENCE implementer replaces this file entirely.
 * Use src/api.ts for all server calls and src/store/cards.ts for persistence.
 */
export default function App() {
  return <main>foundation ok</main>;
}
